---
const { title, titleColor } = Astro.props;
---

<li>
	<h1 class={`mb-8 text-lg font-medium leading-6 sm:text-2xl text-shadow navItem ${titleColor ?? 'dark:text-purple-400 text-sky-400'} `} id="nav-dialog-title">{title}</h1>
	<ul class="flex flex-col items-center w-full">
		<slot />
	</ul>
</li>
